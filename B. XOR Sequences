#include<bits/stdc++.h>
using namespace std;
int main(){
int test;
    cin >> test;
    while (test--) {
        long long a, b;
        cin >> a >> b;
        int x = a ^ b;
        int ans = __builtin_ctz(x);
       // cout << ans << endl;

        cout << (1 << ans) << endl;
    }
}
